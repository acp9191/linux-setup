# Build & development
build-essential        # Compiler and standard build tools
shellcheck             # Static analysis for shell scripts

# Core utilities
ca-certificates        # Trusted CA certificates for HTTPS
curl                   # Transfer data from URLs
wget                   # Download files
unzip                  # Extract ZIP archives
zip                    # Create ZIP archives

# Shell & terminal
zsh                    # Preferred shell
fzf                    # Fuzzy finder
fd-find                # Fast alternative to find
bat                    # Better cat with syntax highlighting
eza                    # Modern alternative to ls
tree                   # Display directory trees
tmux                   # Terminal multiplexer
zoxide                 # Smarter cd
direnv                 # Automatically loads project-specific environment variables

# Git & data
gh                     # GitHub's official command-line interface
git                    # Version control
git-delta              # Better Git diff viewer with syntax highlighting
gnupg                  # GPG encryption and Git commit signing
jq                     # JSON processor
yq                     # YAML processor, analogous to jq

# Search & inspection
ripgrep                # Fast text search
lsof                   # Inspect open files and network connections
ncdu                   # Interactive disk usage analyzer

# Networking
openssh-server         # SSH server
net-tools              # Legacy networking tools such as netstat
